<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div id="dialog">

   </div>

  <div class="field">
    <%= f.label :name, (I18n.t("name")) %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <!--
    <%= f.label :photo %><br />
    <%= f.text_field :photo %>
    -->
    <lable><%= I18n.t"image" %></lable>
    <%= f.file_field :photo %>
  </div>
    <!--
    <div class="display_items">
      <label>子项</label>
      <br/>
      <div class="content">

      </div>

    </div>

    <%= link_to (I18n.t"add_sub_item"), options = {action:"add_display_items"}, html_options = {method: :post, remote:true} %>
    -->
  <div class="actions">
    <%= f.submit(I18n.t("create_page")) %>
  </div>
<% end %>

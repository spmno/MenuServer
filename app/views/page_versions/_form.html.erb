<%= form_for(@page_version) do |f| %>
  <% if @page_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_version.errors.count, "error") %> prohibited this page_version from being saved:</h2>

      <ul>
      <% @page_version.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
